[workspace]
resolver = "2"
members = [
    "symbolica_community",
    "symbolica",
] # TODO: add symbolica_python as well?

[profile.release]
codegen-units = 1
lto = true

[profile.dev-optim]
inherits = "dev"
opt-level = 3
